# Copy this file to .env and fill in real values.
# .env is gitignored -- never commit real secrets.

# Password for the local Neo4j container (user is always "neo4j")
NEO4J_PASSWORD=changeme-dev-password